2026-03-27 | process-supervision (en + it)
  Created: docs/en/dev/supervisor/process-supervision.md
  Created: docs/en/po/supervisor/process-supervision.md
  Created: docs/it/dev/supervisor/process-supervision.md
  Created: docs/it/po/supervisor/process-supervision.md
  Covered: MasterSupervisor, BrokerSupervisor, MemoryManager, signal handling, reconnection strategy, circuit breaker, graceful shutdown, configuration, database schema
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: message publishing (MqttBroadcast facade, MqttMessageJob, sync/async), message subscription & events (MqttMessageReceived, listeners), dashboard & monitoring API (controllers, React frontend), rate limiting, health checking, installation & configuration guide

2026-03-27 | message-publishing (en + it)
  Created: docs/en/dev/publishing/message-publishing.md
  Created: docs/en/po/publishing/message-publishing.md
  Created: docs/it/dev/publishing/message-publishing.md
  Created: docs/it/po/publishing/message-publishing.md
  Covered: MqttBroadcast facade (publish/publishSync), MqttMessageJob (queue dispatch, handle, checkRateLimit, mqtt client creation), RateLimitService (two-layer enforcement, reject/throttle strategies, per-second/per-minute, per-connection/global), MqttClientFactory (ephemeral connections, UUID client IDs), MqttConnectionConfig (validated config VO), helper functions (mqttMessage/mqttMessageSync), topic prefixing, JSON serialization, error handling (fail-fast config errors vs retryable network errors), mqtt_loggers table, queue configuration
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: message subscription & events (MqttMessageReceived, listeners), dashboard & monitoring API (controllers, React frontend), health checking, installation & configuration guide

2026-03-27 | message-subscription (en + it)
  Created: docs/en/dev/subscription/message-subscription.md
  Created: docs/en/po/subscription/message-subscription.md
  Created: docs/it/dev/subscription/message-subscription.md
  Created: docs/it/po/subscription/message-subscription.md
  Covered: MqttMessageReceived event (immutable VO with topic/message/broker/pid), Listener contract interface, MqttListener abstract base class (broker filtering, topic filtering, preProcessMessage hook, JSON-only decode with object validation, queue support via ShouldQueue), Logger built-in listener (optional DB storage, JSON fallback to raw), EventMap trait (event-to-listener mapping), MqttBroadcast::received() dispatch, MqttBroadcastServiceProvider::registerEvents() registration, BrokerSupervisor::handleMessage() with error isolation, BrokerSupervisor::connect() wildcard subscription, published provider stub for custom listeners, queue configuration (listener queue, log queue)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: dashboard & monitoring API (controllers, React frontend), health checking, installation & configuration guide

2026-03-27 | dashboard-monitoring (en + it)
  Created: docs/en/dev/dashboard/dashboard-monitoring.md
  Created: docs/en/po/dashboard/dashboard-monitoring.md
  Created: docs/it/dev/dashboard/dashboard-monitoring.md
  Created: docs/it/po/dashboard/dashboard-monitoring.md
  Covered: HealthController (200/503 health check, memory thresholds, individual checks), DashboardStatsController (aggregated stats, broker counts, message throughput, memory usage, uptime), BrokerController (broker list/detail, connection status state machine, uptime formatting, recent messages), MessageLogController (filtered/paginated logs, topic analytics, JSON parsing, message preview), MetricsController (time-series throughput with gap filling, summary with peak detection), Authorize middleware (Gate-based auth, local bypass), BrokerRepository (database CRUD), MasterSupervisorRepository (cache-based state, multi-driver key listing), route registration (configurable prefix/domain/middleware), React SPA catch-all route
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: Artisan commands (InstallCommand, MqttBroadcastCommand, MqttBroadcastTerminateCommand, MqttBroadcastTestCommand), installation & configuration guide

2026-03-27 | artisan-commands (en + it)
  Created: docs/en/dev/commands/artisan-commands.md
  Created: docs/en/po/commands/artisan-commands.md
  Created: docs/it/dev/commands/artisan-commands.md
  Created: docs/it/po/commands/artisan-commands.md
  Covered: InstallCommand (vendor:publish for config/provider/assets, Laravel 10/11+ provider registration, idempotent install), MqttBroadcastCommand (master name generation via ProcessIdentifier, duplicate prevention, environment resolution, fail-fast config validation, MasterSupervisor + BrokerSupervisor creation, SIGINT handler, blocking monitor loop), MqttBroadcastTerminateCommand (hostname-based process filtering, optional broker targeting, SIGTERM signaling, DB + cache cleanup, ESRCH handling, best-effort semantics), MqttBroadcastTestCommand (synchronous publish via facade), ProcessIdentifier (hostname slugification, static token memoization)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: installation & configuration guide (comprehensive getting-started guide combining all features)

2026-03-27 | dead-letter-queue (en + it)
  Created: docs/en/dev/failed-jobs/dead-letter-queue.md
  Created: docs/en/po/failed-jobs/dead-letter-queue.md
  Created: docs/it/dev/failed-jobs/dead-letter-queue.md
  Created: docs/it/po/failed-jobs/dead-letter-queue.md
  Covered: FailedMqttJob model (configurable table/connection, JSON cast, HasExternalId UUID trait), MqttMessageJob::failed() hook (DLQ persistence on failure), FailedJobController (index with broker/topic filters, show with full detail, retry single/all with 1-min cooldown, destroy, flush/truncate), formatJob() response formatting (100-char message preview, first-line exception preview), DashboardStatsController failed_jobs stats integration, FailedJobs React component (per-job retry/delete, bulk retry/flush, loading states), dashboardApi client methods, mqtt_failed_jobs migration (configurable connection), failure triggers (config error, rate limit reject/throttle, network errors), API routes
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: installation & configuration guide (comprehensive getting-started guide combining all features), ServiceProvider internals (service bindings, event registration, migration loading), Contracts/interfaces (Listener, Pausable, Restartable, Terminable), ListensForSignals trait, deprecated classes (Brokers, BrokerValidator)

2026-03-27 | package-infrastructure (en + it)
  Created: docs/en/dev/infrastructure/package-infrastructure.md
  Created: docs/en/po/infrastructure/package-infrastructure.md
  Created: docs/it/dev/infrastructure/package-infrastructure.md
  Created: docs/it/po/infrastructure/package-infrastructure.md
  Covered: MqttBroadcastServiceProvider (boot/register lifecycle, configure, registerServices, registerMigrations, registerEvents, registerRoutes, registerGate, registerViews, offerPublishing, registerCommands), ServiceBindings trait (MqttClientFactory, BrokerRepository, MasterSupervisorRepository singletons), EventMap trait (MqttMessageReceived → Logger mapping), ListensForSignals trait (SIGTERM/SIGUSR1/SIGUSR2/SIGCONT async handling, pendingSignals queue, processPendingSignals drain), Contracts/interfaces (Listener, Pausable, Restartable, Terminable), Exceptions (MqttBroadcastException static factories, RateLimitExceededException with structured context), helper functions (mqttMessage/mqttMessageSync with function_exists guards), config/mqtt-broadcast.php (connections, environments, dashboard, logs, failed_jobs, MQTT defaults, memory, queue, supervisor, repository), routes/web.php (16 API endpoints + SPA catch-all), publishable provider stub, database schema (mqtt_loggers, mqtt_brokers, mqtt_failed_jobs with all migrations)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: deprecated classes (Brokers, BrokerValidator), installation & configuration quick-start guide

2026-03-27 | dashboard-frontend (en + it)
  Created: docs/en/dev/dashboard/dashboard-frontend.md
  Created: docs/en/po/dashboard/dashboard-frontend.md
  Created: docs/it/dev/dashboard/dashboard-frontend.md
  Created: docs/it/po/dashboard/dashboard-frontend.md
  Covered: React 19 SPA architecture (Vite + laravel-vite-plugin + Tailwind CSS), Blade template bootstrap (window.mqttBroadcast config injection), usePolling generic hook (setInterval + cleanup + state management), useDashboard domain hooks (useStats, useBrokers, useMessages, useThroughput, useFailedJobs), dashboardApi Axios client (all 14 methods with typed responses), Dashboard root component (tab state, stats cards, layout orchestration), Navigation component (TabType, TabButton with badge), StatsCard (variant coloring, trend support), ThroughputChart (Recharts LineChart, ResponsiveContainer, theme-aware styling), BrokerTable (connection_status state machine badges, uptime, 24h counts), MessageLog (date-fns relative timestamps, logging-disabled state), FailedJobs component (per-job retry/delete, bulk retry-all/flush-all, confirm dialog, loading states), DocsPage (CommandBlock, TroubleshootItem, ChecklistItem, ResourceCard helper components), ThemeToggle + useTheme (dark/light/system, localStorage persistence, prefers-color-scheme detection), UI primitives (Card/Badge/Button with CVA variants), cn() utility (clsx + tailwind-merge), TypeScript type system (DashboardStats, Broker, MessageLog, Topic, ThroughputData, FailedJob, MetricsSummary)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: deprecated classes (Brokers, BrokerValidator)

2026-03-27 | installation-configuration (en + it)
  Created: docs/en/dev/getting-started/installation-configuration.md
  Created: docs/en/po/getting-started/installation-configuration.md
  Created: docs/it/dev/getting-started/installation-configuration.md
  Created: docs/it/po/getting-started/installation-configuration.md
  Covered: Composer installation with auto-discovery, InstallCommand (publish config/provider/assets, Laravel 10/11+ provider registration), migration auto-loading (mqtt_loggers, mqtt_brokers, mqtt_failed_jobs tables), .env configuration (all MQTT_* vars), dashboard gate setup (viewMqttBroadcast, local bypass, Authorize middleware), supervisor startup, full configuration reference (connections, environments, dashboard, logging, DLQ, MQTT defaults, memory management, queue, supervisor, repository settings), error handling (installation/configuration/runtime), production deployment checklist (queue worker, Redis, gate, process manager, supervisord example)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Still needs coverage: deprecated classes (Brokers, BrokerValidator)

2026-03-27 | deprecations-migration (en + it)
  Created: docs/en/dev/migration/deprecations-migration.md
  Created: docs/en/po/migration/deprecations-migration.md
  Created: docs/it/dev/migration/deprecations-migration.md
  Created: docs/it/po/migration/deprecations-migration.md
  Covered: Brokers class (deprecated since 2.5.0, monolithic broker process manager, make/client/monitor/terminate methods, ListensForSignals usage, process lookup), BrokerValidator class (deprecated since 3.0, basic config validation, InvalidBrokerException dependency now removed), full migration paths (Brokers → MasterSupervisor + BrokerSupervisor + MqttClientFactory, BrokerValidator → MqttConnectionConfig), before/after code examples for each migration, Mermaid architecture comparison diagram, removal timeline, runtime deprecation notices (trigger_deprecation)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  All identified features and deprecated classes are now documented. Remaining opportunities: deepen existing docs with additional edge cases, cross-reference between docs, or document any new features added in future development.

2026-03-27 | testing-infrastructure (en + it)
  Created: docs/en/dev/testing/testing-infrastructure.md
  Created: docs/en/po/testing/testing-infrastructure.md
  Created: docs/it/dev/testing/testing-infrastructure.md
  Created: docs/it/po/testing/testing-infrastructure.md
  Covered: TestCase base class (Orchestra Testbench bootstrap, SQLite in-memory DB, config defaults, setMqttConfig/getProtectedProperty/requiresBroker helpers), Pest.php bootstrap (uses directive, Mockery cleanup), MockMqttClient (drop-in MQTT client double, publish/subscribe recording, assertPublished/assertNotPublished assertions, connection state enforcement), BrokerAvailability (cached broker detection, MQTT_BROKER_AVAILABLE env var, live connection attempt, diagnostic socket fallback), test patterns (Mockery factory mocking, Queue::fake with protected property assertions, HTTP controller tests, andReturnUsing for real model returns, proc_open integration tests), dev dependencies (Pest 4, Testbench 9/10, PHPUnit 10/11/12, Larastan, Collision), composer scripts (test, test-coverage, pint, analyse)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen existing docs (cross-references between docs, additional edge cases), document MqttBroadcast::css()/js()/manifest() asset helper methods (currently undocumented but used by dashboard Blade template)

2026-03-27 | asset-pipeline (en + it)
  Created: docs/en/dev/dashboard/asset-pipeline.md
  Created: docs/en/po/dashboard/asset-pipeline.md
  Created: docs/it/dev/dashboard/asset-pipeline.md
  Created: docs/it/po/dashboard/asset-pipeline.md
  Covered: Vite configuration (entry points, buildDirectory, deterministic filenames, @ alias), laravel-vite-plugin integration (custom build directory, manifest resolution), manifest.json structure (isEntry, file, css dependencies), MqttBroadcast::css() (manifest-based link tag generation, entry filtering, CSS deduplication), MqttBroadcast::js() (manifest-based script module tag generation), MqttBroadcast::manifest() (two-step path resolution: published → package → empty fallback), Blade template integration (window.mqttBroadcast config injection, @vite directive with custom build directory), asset publishing (mqtt-broadcast-assets and laravel-assets tags, offerPublishing in ServiceProvider), build commands (npm run dev/build), error handling (missing manifest, invalid JSON, unpublished assets, dev server not running)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen existing docs with cross-references between documents, add Facade docblock entries for css()/js() methods, document advanced MQTT connection defaults and memory management configuration in greater detail

2026-03-27 | connection-management (en + it)
  Created: docs/en/dev/publishing/connection-management.md
  Created: docs/en/po/publishing/connection-management.md
  Created: docs/it/dev/publishing/connection-management.md
  Created: docs/it/po/publishing/connection-management.md
  Covered: MqttConnectionConfig (immutable value object, fromConnection/fromArray named constructors, default merging via array_merge with null stripping, validation chain: host/port/qos/timeout/alive_interval/auth credentials, toArray() serialization), MqttClientFactory (create/createFromConfig client instantiation, getConnectionSettings/getConnectionSettingsFromConfig auth+TLS builder, three-tier client ID resolution: explicit > config > UUID, configured-but-not-connected pattern), integration points (MqttMessageJob ephemeral publishers with random UUID, BrokerSupervisor long-lived subscribers with fixed ID, MqttBroadcastCommand startup validation), full configuration reference (per-connection keys + global defaults), error handling (all 10 validation exception patterns with recovery instructions, fail-fast vs retry distinction in MqttMessageJob), Mermaid diagrams (config resolution flow, client creation sequence, client ID strategy)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen MemoryManager documentation (three-tier warning system, grace period, auto-restart callbacks), dedicated HTTP API reference document (request/response schemas for all 16 endpoints), Repositories deep-dive (MasterSupervisorRepository cache driver abstraction, BrokerRepository silent-fail pattern)

2026-03-27 | repository-pattern (en + it)
  Created: docs/en/dev/infrastructure/repository-pattern.md
  Created: docs/en/po/infrastructure/repository-pattern.md
  Created: docs/it/dev/infrastructure/repository-pattern.md
  Created: docs/it/po/infrastructure/repository-pattern.md
  Covered: BrokerRepository (create/find/all/touch/delete/deleteByPid/generateName, silent-fail pattern, heartbeat lifecycle, BrokerProcess model, mqtt_brokers schema with all columns), MasterSupervisorRepository (update/find/forget/all/names, cache key format mqtt-broadcast:master:{name}, auto updated_at, configurable TTL), multi-driver cache key listing (Redis KEYS command, file driver glob+deserialize, Memcached limitation, array driver reflection, default empty fallback), ServiceBindings singleton registration, integration points (BrokerSupervisor create/touch/delete, MasterSupervisor persist/forget, DashboardStatsController/HealthController/BrokerController reads, MqttBroadcastTerminateCommand cleanup), configuration reference (cache_ttl, cache_driver, heartbeat_column, stale_threshold), Mermaid diagrams (consumer dependency flowchart, startup/monitor/shutdown sequence diagram)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen MemoryManager documentation (three-tier warning system, grace period, auto-restart callbacks), dedicated HTTP API reference document (request/response schemas for all 16 endpoints)

2026-03-27 | memory-management (en + it)
  Created: docs/en/dev/supervisor/memory-management.md
  Created: docs/en/po/supervisor/memory-management.md
  Created: docs/it/dev/supervisor/memory-management.md
  Created: docs/it/po/supervisor/memory-management.md
  Covered: MemoryManager class (tick-based monitoring pattern, loopIteration counter, gc_interval modulo check), performGarbageCollection (gc_collect_cycles, freed memory logging, noise suppression), three-tier warning system (80% warning, 100% error, restart after grace period), handleThresholdExceeded (grace period tracking via thresholdExceededAt timestamp, microtime elapsed check, onRestart callback invocation), auto-restart flow (MasterSupervisor restart() → terminate(0), process manager restarts), BrokerSupervisor integration (onRestart: null, logging only, no self-restart), getMemoryStats (current_mb/peak_mb/current_bytes/peak_bytes), reset() for testing/re-init, persist() memory stats in cache state, configuration reference (gc_interval, threshold_mb, auto_restart, restart_delay_seconds with env vars), tuning guidelines, Mermaid diagrams (tick decision flowchart, supervisor hierarchy sequence diagram, memory state machine)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: dedicated HTTP API reference document (request/response schemas for all 16 endpoints), RateLimitService deep-dive (two-layer enforcement internals, reject vs throttle strategy implementation details)

2026-03-27 | rate-limiting (en + it)
  Created: docs/en/dev/publishing/rate-limiting.md
  Created: docs/en/po/publishing/rate-limiting.md
  Created: docs/it/dev/publishing/rate-limiting.md
  Created: docs/it/po/publishing/rate-limiting.md
  Covered: RateLimitService class (constructor with configurable cache driver, RateLimiter wrapper), allows() non-destructive check (per-second then per-minute tooManyAttempts), attempt() check+hit+throw, hit() dual-counter increment (1s and 60s TTL), remaining() most-restrictive-window min calculation, availableIn() seconds-until-reset for both windows, clear() counter reset, handleRateLimitExceeded() strategy branching (reject → RateLimitExceededException, throttle → return delay), getKey() per-connection vs global cache key namespace, getLimitsForConnection() two-tier resolution (per-connection override → global default), isEnabled() config check, RateLimitExceededException (connection/limit/window/retryAfter structured context), MqttMessageJob::checkRateLimit() job-level enforcement (allows+hit or throttle release or reject attempt), two-layer enforcement architecture (facade + job), per-connection overrides from connection config, null limit handling (effectively unlimited), cache key format (mqtt_rate_limit:{connection}:{window}), all configuration keys with env vars
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: dedicated HTTP API reference document (request/response schemas for all 16 endpoints)

2026-03-27 | http-api-reference (en + it)
  Created: docs/en/dev/dashboard/http-api-reference.md
  Created: docs/en/po/dashboard/http-api-reference.md
  Created: docs/it/dev/dashboard/http-api-reference.md
  Created: docs/it/po/dashboard/http-api-reference.md
  Covered: All 15 API endpoints with full request/response schemas, Authorize middleware (Gate-based auth, local bypass, 403 handling), HealthController (200/503, memory check status values: pass/warn/critical/unknown, health criteria), DashboardStatsController (aggregated stats, message counts conditional on logs.enable), BrokerController (index with computed connection_status state machine: connected/idle/reconnecting/disconnected based on heartbeat age thresholds 30s/120s, show with recent_messages, uptime_human formatting), MessageLogController (index with broker/topic/limit filters, show with is_json/message_parsed, topics with 24h top-20), MetricsController (throughput with period=hour/day/week gap-filling, time format per period, summary with peak_minute detection), FailedJobController (index with broker/topic/limit filters using external_id UUID, show with full exception/message, retry single, retryAll with 1-min cooldown, destroy 204, flush truncate), Dashboard SPA catch-all, configuration reference (prefix/domain/middleware/logs.enable/queue.name/memory.threshold_mb/master_supervisor.name), error handling matrix, Mermaid diagrams (auth flow, connection_status state machine, health check decision flow, failed job retry sequence)
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen existing docs with cross-references between documents, document edge cases around MySQL-specific DATE_FORMAT() in MetricsController (SQLite compatibility gap)

2026-03-27 | testing-infrastructure deepening (en + it)
  Updated: docs/en/dev/testing/testing-infrastructure.md
  Updated: docs/en/po/testing/testing-infrastructure.md
  Updated: docs/it/dev/testing/testing-infrastructure.md
  Updated: docs/it/po/testing/testing-infrastructure.md
  Fixed: BrokerProcess factory examples used wrong attribute names (broker_name/connection_status/master_name → name/connection/working) in both dev docs (EN + IT)
  Added: BrokerProcessFactory documentation with stopped()/stale() states, usage examples, and combination patterns
  Added: MqttLoggerFactory documentation with default values, JSON/raw message examples, configurable connection/table
  Added: HasExternalId trait documentation (auto-UUID generation, route key override, correct API URL usage in tests)
  Added: Key Components table expanded with factory files and HasExternalId trait
  Added: PO docs updated with business rules about test data factories and UUID identifiers
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen existing docs with cross-references between documents, document MetricsController MySQL-specific DATE_FORMAT() (SQLite compatibility gap), expand MqttListener::preProcessMessage() documentation with parameter signature details

2026-03-27 | process-supervision deepening (en + it)
  Updated: docs/en/dev/supervisor/process-supervision.md
  Updated: docs/en/po/supervisor/process-supervision.md
  Updated: docs/it/dev/supervisor/process-supervision.md
  Updated: docs/it/po/supervisor/process-supervision.md
  Added: BrokerSupervisor $options constructor parameter documentation with three-tier config resolution chain
  Added: validateReconnectionConfig() throws \InvalidArgumentException (not MqttBroadcastException) — documented distinction
  Added: Exact exponential backoff formula pow(2, retryCount - 1) with delay table
  Added: Passive backoff mechanism explained (timestamp comparison, no sleep)
  Added: Circuit breaker hard-exit behavior (terminate(1) in shouldRetry()) with firstFailureAt accumulation through soft resets
  Added: Soft vs hard mode detailed behavioral distinction (retryCount reset vs terminate, firstFailureAt preservation)
  Added: Output callback system documentation (setOutput on MasterSupervisor, constructor param on BrokerSupervisor, [$brokerName] prefix wrapping)
  Added: MasterSupervisor::monitor() vs loop() separation for testability
  Added: Config resolution Mermaid flowchart diagram
  Added: PO docs expanded with soft/hard retry modes, circuit breaker safety net, invalid config edge case, output structure
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: document MetricsController MySQL-specific DATE_FORMAT() (SQLite compatibility gap), expand MqttListener::preProcessMessage() documentation with parameter signature details, document MqttBroadcast::getTopic() public method, document MqttMessageJob $cleanSession and retain config key, document auth key requirement in MqttConnectionConfig

2026-03-27 | connection-management deepening (en + it)
  Updated: docs/en/dev/publishing/connection-management.md
  Updated: docs/en/po/publishing/connection-management.md
  Updated: docs/it/dev/publishing/connection-management.md
  Updated: docs/it/po/publishing/connection-management.md
  Added: TLS and authentication in ConnectionSettings — exact builder code, TLS-only-with-auth design decision, cleanSession two-tier resolution
  Added: MqttBroadcast::getTopic() documentation — prefix concatenation, usage in MqttMessageJob::handle() and MqttListener::getTopic(), validation behavior
  Added: Publisher connection lifecycle (MqttMessageJob::mqtt()) — three-step flow (create client, get settings, conditional connect), $cachedQos and $cachedRetain dispatch-time caching, retain read directly from connection config not MqttConnectionConfig
  Added: `auth` key to per-connection config table (was missing — controls whether auth validation and TLS are applied)
  Added: `qos`, `retain`, `clean_session` as per-connection override keys in config table (were only shown under defaults)
  Added: cleanSession behavioral difference: publisher always true, subscriber uses config value
  Added: Publisher connection lifecycle sequence diagram (Mermaid)
  Added: Topic prefix resolution flowchart (Mermaid)
  Added: Key Components table expanded with requiresAuth(), retain(), cleanSession(), prefix(), useTls(), selfSignedAllowed() accessors and MqttBroadcast::getTopic()
  Added: PO docs expanded with auth opt-in rule, TLS-with-auth-only rule, topic prefixing, retain per-connection, clean session publisher/subscriber distinction, empty prefix edge case, QoS/retain caching edge case
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: document MetricsController MySQL-specific DATE_FORMAT() (SQLite compatibility gap), expand MqttListener::preProcessMessage() documentation with parameter signature details

2026-03-27 | message-subscription deepening (en + it)
  Updated: docs/en/dev/subscription/message-subscription.md
  Updated: docs/en/po/subscription/message-subscription.md
  Updated: docs/it/dev/subscription/message-subscription.md
  Updated: docs/it/po/subscription/message-subscription.md
  Added: preProcessMessage() full signature (?string $topic = null, ?object $obj = null): bool — documented that handle() calls it with NO args (pre-decode gating), params exist for subclass flexibility
  Added: Logger vs MqttListener contract distinction — Logger implements ShouldQueue directly (no Listener contract, no filtering), MqttListener implements Listener contract (strict JSON-object filter)
  Added: Topic matching is exact string compare, not MQTT wildcard matching — only literal '*' is special-cased
  Added: MqttListener default property values ($handleBroker='local', $topic='*') and viaQueue() routing
  Added: Logger viaQueue() routing, MqttLogger model details (HasExternalId UUID, configurable connection/table, json cast)
  Added: MqttMessageReceived::getPid() documentation — not populated by received(), reserved for internal/testing
  Added: Database schema expanded with external_id UUID column and HasExternalId trait explanation
  Added: handleMessage() message truncation to 100 chars for logging
  Added: New Mermaid diagram: Logger vs MqttListener architecture comparison flowchart
  Added: Enhanced Listener Filtering Pipeline diagram with exact string compare annotations and JSON depth 512
  Added: FilteredListener example showing preProcessMessage() usage (maintenance window gating)
  Added: Error handling table expanded with preProcessMessage() returns false scenario
  Added: PO docs expanded with two listener architectures rule, pre-processing gate, UUID identification, JSON array edge case
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: document MetricsController MySQL-specific DATE_FORMAT() (SQLite compatibility gap)

2026-03-27 | dashboard-monitoring deepening (en + it)
  Updated: docs/en/dev/dashboard/dashboard-monitoring.md
  Updated: docs/en/po/dashboard/dashboard-monitoring.md
  Updated: docs/it/dev/dashboard/dashboard-monitoring.md
  Updated: docs/it/po/dashboard/dashboard-monitoring.md
  Added: MetricsController MySQL-specific DATE_FORMAT() / DATE() incompatibility with SQLite and PostgreSQL — full equivalence table for all three methods, impact scope, and cross-database comparison with MessageLogController::topics()
  Added: DashboardStatsController internals — 6-step aggregation walkthrough, calculateMemoryUsagePercent() division-by-zero guard, calculateUptime() Carbon parse with absolute flag
  Added: Queue::size() inconsistency — DashboardStatsController wraps in try/catch (sync driver safe), HealthController does not (potential unhandled exception)
  Added: N+1 query patterns — BrokerController::index() 2N+1 queries with logging enabled, BrokerController::show() loads all brokers to find one
  Added: HealthController helper methods — getMasterSupervisorData() array/object dual handling, checkMemoryStatus() three-tier status with unique 'unknown' state
  Added: New Mermaid sequence diagram — DashboardStatsController aggregation flow showing all data sources and conditional branches
  Added: Key Components table expanded with HealthController::getMasterSupervisorData(), checkMemoryStatus(), DashboardStatsController::calculateMemoryUsagePercent(), calculateUptime()
  Added: Error handling expanded with sync queue driver and MySQL-only metrics entries
  Added: PO docs expanded with MySQL dependency for metrics, queue size sync driver edge case, non-MySQL database edge case
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: all identified features and edge cases are now documented. The codebase is comprehensively covered across 17 dev docs and corresponding PO docs. Future opportunities: document any new features added in development, or add cross-reference links between related documents.

2026-03-27 | message-publishing deepening (en + it)
  Updated: docs/en/dev/publishing/message-publishing.md
  Updated: docs/en/po/publishing/message-publishing.md
  Updated: docs/it/dev/publishing/message-publishing.md
  Updated: docs/it/po/publishing/message-publishing.md
  Added: DLQ integration — failed() hook persisting to mqtt_failed_jobs table on permanent failure
  Added: API surface inconsistencies table — publish() string vs mixed, helper 'local' vs facade 'default' broker defaults
  Added: Constructor config caching — $cachedQos/$cachedRetain read from per-connection config (not defaults.connection.*), dispatch-time vs execution-time distinction
  Added: $cleanSession parameter documentation — always true for publishers, not exposed via facade, contrasts with subscriber behavior
  Added: Conditional connection logic in mqtt() method — getConnectionSettings() may return null settings, isConnected() guard in handle()
  Added: getTopic() validates broker config — can throw MqttBroadcastException on unconfigured broker
  Added: Key Components table expanded with MqttMessageJob::failed() and FailedMqttJob model
  Added: Error handling table expanded with permanent failure DLQ persistence
  Added: PO docs expanded with DLQ persistence rule, helper vs facade broker default mismatch, QoS/retain dispatch-time capture, clean session rule, no-auth edge case, config-change-after-dispatch edge case
  Skipped: sales/ (infrastructure feature, not customer-facing)
  Remaining opportunities: deepen artisan-commands, dead-letter-queue, package-infrastructure, dashboard-frontend, installation-configuration, deprecations-migration, asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | artisan-commands deepening (en)
  Updated: docs/commands/artisan-commands.md
  Added: PHP 8 #[AsCommand] attribute usage differences across commands — metadata and $signature visibility inconsistencies
  Added: Dependency injection patterns table — container injection vs static calls vs self-contained
  Added: MqttBroadcastTestCommand uses MqttBroadcast class directly, not the Facade
  Added: registerInConfigApp() identical branches analysis — both if/else do the same str_replace
  Added: Output callback architecture — $type parameter unused, connection name prefixing for broker supervisors
  Added: No return after monitor() — implicit null return, undefined exit code edge case
  Added: Terminate command components->task() visual wrapper and &$result by-reference pattern
  Added: ProcessIdentifier Internals section — pid(), hostname(), generateName() with token generation, prefix handling, empty string edge case
  Added: Mermaid diagrams — ProcessIdentifier name generation flowchart, output callback architecture sequence diagram
  Added: Enhanced install flow diagram with provider-already-registered check
  Added: Error handling table expanded with monitor() unexpected return, test command always-SUCCESS exit code, no-processes-on-host scenario
  Added: Key Components table expanded with ProcessIdentifier::pid(), registerCommands() single-call detail
  Remaining opportunities: deepen dead-letter-queue, package-infrastructure, dashboard-frontend, installation-configuration, deprecations-migration, asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | dead-letter-queue deepening
  Updated: docs/failed-jobs/dead-letter-queue.md
  Added: HasExternalId::booted() static override analysis — null coalesce behavior, trait conflict risk, pre-set external_id preservation
  Added: retry() three-query pattern — increment + update + fresh = 4 queries per retry, no batching
  Added: retryAll() memory and query scaling — no chunking, 1+3N queries, memory exhaustion risk for large backlogs
  Added: index() extra count query — unfiltered COUNT(*) on every request regardless of filters
  Added: formatJob() byte-length truncation — strlen/substr byte semantics, multibyte UTF-8 truncation risk with raw pre-encoded strings
  Added: Frontend error handling asymmetry — handleDelete has no try/catch, refetch unreachable on failure
  Added: Retain AND cleanSession lost on retry — expanded existing section to cover cleanSession default behavior
  Added: FailedMqttJob model runtime config resolution — getConnectionName/getTable read at call time, $fillable/$casts details
  Added: Key Components table expanded with getConnectionName(), getTable(), FailedJob TS interface, useFailedJobs hook
  Added: Mermaid sequence diagram — retry() and retryAll() query flow visualization
  Remaining opportunities: deepen package-infrastructure, dashboard-frontend, installation-configuration, deprecations-migration, asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | package-infrastructure deepening
  Updated: docs/infrastructure/package-infrastructure.md
  Added: Facade resolution quirk — static methods behind Facade accessor, auto-discovery alias, three calling paths (Facade/helper/direct) with Mermaid diagram
  Added: MqttBroadcast class full method table — publish/publishSync/received/getTopic/css/js/manifest/validateBrokerConfiguration
  Added: Type inconsistency — publish() accepts string, publishSync() accepts mixed
  Added: validateBrokerConfiguration() minimal validation — only host/port, no auth/TLS/type checks
  Added: Dual rate-limit check — facade-level + job-level enforcement for async publishing
  Added: Helper function broker default mismatch — 'local' vs 'default', potential MqttBroadcastException
  Added: ServiceBindings self-binding only — all numeric keys, public property, no interface→implementation
  Added: Authorize middleware internals — Gate::allows() redundant argument, plain 403 response (not JSON), local bypass
  Added: Publishable provider stub boot() ordering — parent::boot() registers routes before config overrides in stub
  Added: Signal deduplication — identical key/value prevents duplicate signal processing, FIFO drain order via Arr::first()
  Added: Signal drain order — terminate processes before restart/pause when both queued
  Added: ListensForSignals contract assumption — no method existence enforcement, relies on dynamic call
  Added: Enhanced signal sequence diagram — shows multi-signal drain with ordering
  Added: mqtt_failed_jobs schema correction — broker (not connection), added qos/retain/retried_at/retry_count columns
  Added: mqtt_loggers composite index detail — replaces original single broker index (migration 2024_11_03)
  Added: Platform requirements table — PHP 8.3+, ext-pcntl, ext-posix, Laravel 11-13, php-mqtt/client
  Added: Windows incompatibility note for supervisor system
  Added: Legacy password config key — Str::random(32) regenerates per request, not used by any code path
  Added: Migration connection behavior — Schema::connection() reads config at migration time, not runtime
  Added: Key Components table expanded with MqttBroadcast class, Facade, Authorize middleware, composer.json entries
  Remaining opportunities: deepen dashboard-frontend, installation-configuration, deprecations-migration, asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | dashboard-frontend deepening
  Updated: docs/dashboard/dashboard-frontend.md
  Fixed: TabType was documented as 3 tabs (dashboard/failed-jobs/docs) but actual code only has 2 tabs (dashboard/failed-jobs) — DocsPage.tsx is dead code, not imported by any component
  Fixed: Component tree showed "docs" tab rendering DocsPage — removed from tree and navigation flow diagram
  Fixed: window.mqttBroadcast config table — basePath is injected by Blade but NOT declared in TypeScript Window interface, marked as dead property
  Added: TypeScript vs Blade mismatch — basePath not in TS Window interface, CSRF token injected but not consumed by Axios client
  Added: usePolling internals — stale closure caveat (fetchFn not in useEffect dependency array), no request deduplication, error preservation behavior, disabled mode early-return
  Added: Dual logging check — Dashboard uses stats.messages.logging_enabled (API), useMessages/MessageLog use window.mqttBroadcast.loggingEnabled (Blade config), technically independent
  Added: StatsCard variant mapping table — own variant system (default/success/warning/danger) separate from Badge CVA variants, threshold-based variant selection for each card
  Added: API client ID type distinction — broker/message use number (auto-increment), failed jobs use string (UUID via HasExternalId)
  Added: Unused API methods — getBroker(), getMessage(), getTopics(), getMetricsSummary() defined but not called by any component
  Added: FailedJobs state management — Set<string> per-job tracking, bulkLoading shared state, handleDelete has no try/catch (error handling asymmetry), confirm() for flush
  Added: ThemeToggle vs useTheme — toggle only supports dark/light (no UI for system mode despite hook supporting it), classList removal prevents accumulation
  Added: ThroughputChart theming — HSL CSS custom properties for theme-aware Recharts styling
  Added: Button asChild prop declared but unused (no Slot implementation)
  Added: Hardcoded defaults table — message limit 20, failed jobs limit 50, scroll heights, chart height, throughput period
  Added: BrokerTable empty state with artisan command hint
  Added: Type system deep-dive — all fields documented with which are displayed vs unused (uptime_seconds, working, created_at_human, retried_at, last_message_at)
  Added: main.tsx CSS import via relative path traversal, noscript fallback in Blade template
  Added: Mermaid diagrams — FailedJobs action sequence diagram (showing error handling asymmetry), usePolling stale closure flowchart
  Added: Fixed navigation state diagram to 2-tab model
  Added: Error handling table expanded with empty states, root element missing, concurrent poll requests, per-component error rendering
  Remaining opportunities: deepen installation-configuration, deprecations-migration, asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | installation-configuration deepening
  Updated: docs/getting-started/installation-configuration.md
  Published to Outline: updated "Getting Started" doc (id: 4a984347-050a-4003-9530-ffa3f7d7de48)
  Added: Provider registration two-layer idempotency check analysis — config/app.php read first even on Laravel 11+, redundant but harmless
  Added: registerInConfigApp() identical branches — if/else with same str_replace body
  Added: Published stub boot ordering — parent::boot() dispatches to stub's registerGate() via polymorphism
  Added: registerMigrations() runningInConsole() gate — HTTP requests skip migration discovery
  Added: Authorize middleware redundant user argument — Gate::allows passes user explicitly but Gate auto-injects, explicit arg ignored
  Added: Unauthorized response is plain text 403, not JSON
  Added: Route caching caveat — registerRoutes() skips when cached, must clear cache after path/domain changes
  Added: All per-connection keys table — auth, qos, retain, clean_session, rate_limiting (previously undocumented)
  Added: Config merge behavior — null-stripping array_filter before array_merge with defaults, edge cases documented
  Added: Rate Limiting config section — entirely missing from published config file, phantom config keys read by RateLimitService, enabled defaults to true but null limits = unlimited
  Added: Legacy password config key — Str::random(32) regenerates per request, not used by any code path
  Added: Config Resolution Flow mermaid diagram — fromConnection validation chain
  Added: Validation Two-Tier Architecture section — facade-level (isset only) vs supervisor-level (full range/type validation), port 99999 gap
  Added: config/app.php unreadable edge case in installation errors
  Added: Auth validation only when auth=true, port out of range, QoS out of range, zero timeout errors
  Added: Rate limit exceeded runtime error
  Added: Service Provider Lifecycle sequence diagram — register/boot phases with method dispatch order
  Added: Key Components table expanded with registerInBootstrapProviders, registerInConfigApp, registerRoutes, registerMigrations, offerPublishing, Authorize.handle(), MqttConnectionConfig.fromConnection()
  Added: Helper function broker default mismatch note ('local' vs 'default')
  Remaining opportunities: deepen deprecations-migration, asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | deprecations-migration deepening
  Updated: docs/migration/deprecations-migration.md
  Added: BrokerValidator is runtime-broken — InvalidBrokerException import causes Class not found fatal (autoloader fails before deprecation notice fires)
  Added: throw_unless eager evaluation — exception instantiated even on success path
  Added: Brokers internals section — public property exposure ($broker, $client, $output), name generation (Str::random(4) memoized, precursor to ProcessIdentifier), client() redundant DB lookup (find by name when $this->broker already exists)
  Added: Auto-connect vs no-connect split — auth=true auto-connects with full settings, auth=false returns disconnected client; monitor() then bare-connects with no settings
  Added: DB read per tick analysis — $this->broker->working cached attribute never refreshed, pause/resume signals fatal (pause()/restart()/continue() methods not implemented on Brokers)
  Added: Fixed sleep(1) loop, two-method exit indirection (exit→exitProcess for testability), terminateByPid DB-only cleanup
  Added: Signal handling correction — ListensForSignals registers all 4 signals (SIGTERM/SIGUSR1/SIGUSR2/SIGCONT) but Brokers only implements terminate(), other signals cause method-not-found fatal
  Added: Validation comparison table — BrokerValidator (3 checks, isset-only) vs MqttConnectionConfig (10+ checks with type/range validation, default merging, immutable VO return)
  Added: Validation pipeline comparison Mermaid diagram
  Added: Behavioral differences table — memory management, reconnection, circuit breaker, loop interval, signal support, client connection, termination cleanup, output routing, name generation
  Added: Old vs new monitor loop sequence diagram comparison
  Added: Error handling table — BrokerValidator fatal, invalid port/QoS silently accepted, SIGUSR1/2 fatal, connection loss, memory exhaustion, output callback null
  Added: Key Components table expanded with Brokers::name(), Brokers::terminateByPid(), Brokers::terminate(), Brokers::exitProcess(), ProcessIdentifier::generateName()
  Skipped: Outline publish (plans/doc.md does not exist — no publishing instructions available)
  Remaining opportunities: deepen asset-pipeline, repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | asset-pipeline deepening
  Updated: docs/dashboard/asset-pipeline.md
  Added: Hot file detection mechanism — public/hot created by npm run dev, stale hot file scenario, not cleaned up on crash
  Added: PostCSS pipeline — tailwindcss + autoprefixer two-stage processing
  Added: Tailwind config deep-dive — darkMode class strategy, HSL CSS custom properties design system, content scanning paths, @layer base specificity
  Added: TypeScript configuration — dual tsconfig (app + node), strict mode flags, noEmit (type-check only, esbuild transpiles), @/* path alias duplication between vite.config.ts and tsconfig.json
  Added: CSS import chain — main.tsx imports CSS via relative path traversal (../../../../), creates bundle-time dependency, explains manifest css key on JS entry
  Added: Manifest fields table — name/names fields present but unused by css()/js() helpers
  Added: css() unique() is load-bearing — mqtt-dashboard.css appears twice (CSS entry file + JS entry css array), deduplication prevents duplicate <link> tags
  Added: css()/js() implode indentation inconsistency — 8 spaces vs 4 spaces in generated HTML
  Added: css() flatMap collection walkthrough — step-by-step showing raw values from both entries, filter/unique/extension stages
  Added: npm run preview command — serves production build locally, not documented before
  Added: Re-publishing --force caveat — vendor:publish doesn't overwrite by default, stale assets after package update
  Added: Version pinning table — Vite 8, laravel-vite-plugin 3, React 19, Tailwind 3, TypeScript 5
  Added: Deterministic filenames cache-busting gap — no content hashing, browser may serve stale assets
  Added: refresh: true — Blade template changes trigger full page reload (not HMR)
  Added: file_get_contents permission error handling — emits warning, json_decode(false) → null → []
  Added: #mqtt-dashboard-root missing from DOM — main.tsx throws, noscript fallback doesn't cover this
  Added: Host app laravel-vite-plugin version mismatch risk
  Added: Key Components table expanded with tailwind.config.js, postcss.config.js, tsconfig.json, tsconfig.node.json, CSS entry point, JS entry point, public/hot
  Added: Mermaid diagrams — CSS Processing Pipeline flowchart, css() Collection Logic flowchart
  Skipped: Outline publish (plans/doc.md does not exist — no publishing instructions available)
  Remaining opportunities: deepen repository-pattern, memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | repository-pattern deepening
  Updated: docs/infrastructure/repository-pattern.md
  Added: Master supervisor name mismatch — ProcessIdentifier::generateName('master') produces 'master-hostname-xxxx' but HealthController/DashboardStatsController look up config name 'master', find() never matches, always reports unhealthy/zero
  Added: Cache state key mismatch — persist() stores memory_mb/peak_memory_mb/supervisors but controllers read memory/supervisors_count, values always null/0
  Added: Unit mismatch — controllers apply bytes-to-MB conversion on values already stored in MB by MemoryManager
  Added: Duplicate master detection broken — ProcessIdentifier generates unique token per process, find() checks own name not existing master's name
  Added: File driver key listing broken — flat glob misses Laravel's nested hash directories, getKeyFromFile reads 'key' field that Laravel FileStore doesn't store
  Added: Redis KEYS command production caveat — blocks server on large keysets
  Added: Two name generators comparison table — BrokerRepository (fresh token) vs ProcessIdentifier (memoized static token)
  Added: Phantom config keys — master_supervisor.cache_driver (never read), repository.broker.stale_threshold (never read, all consumers hardcode 2 min vs configured 5 min), repository.broker.heartbeat_column (never read)
  Added: BrokerController::show() bypasses repository — loads all() then firstWhere('id') in memory, no findById() exists
  Added: BrokerController::index() O(n) message queries — 2 queries per broker against mqtt_loggers when logging enabled
  Added: MasterSupervisorRepository::all() sequential reads — find() per name, no Cache::many() batching
  Added: ServiceBindings self-binding note — numeric keys, concrete classes only, cannot swap via container
  Added: TTL constructor caching — singleton freezes cache_ttl at resolution time
  Added: Cache driver configuration disconnect — repository uses Cache facade (app default) and config('cache.default'), not the mqtt-broadcast config key
  Added: BrokerController::determineConnectionStatus() 4-tier state machine with hardcoded thresholds
  Added: Corrected cache state structure — actual keys (memory_mb, peak_memory_mb, supervisors), no started_at field persisted
  Added: Configuration table expanded with "Actually Used By" column exposing phantom config keys
  Added: Mermaid diagrams — enhanced flowchart with name mismatch highlighting, expanded sequence diagram showing name generation + controller read failure, new connection status state diagram
  Skipped: Outline publish (plans/doc.md does not exist — no publishing instructions available)
  Remaining opportunities: deepen memory-management, rate-limiting, or http-api-reference docs

2026-03-27 | memory-management deepening
  Updated: docs/supervisor/memory-management.md
  Added: memory_get_usage(true) vs false distinction — real OS allocation in chunks, step-function behavior, threshold triggers may appear "sudden"
  Added: Config read on every GC tick, not cached — runtime hot-reconfiguration possible but config resolution overhead per tick
  Added: Grace period quantization — effective grace period is max(restart_delay_seconds, gc_interval * sleep), default 10s actually takes ~100s with gc_interval=100
  Added: Recovery edge case — memory dropping from >=100% to 80-100% range logs both warning and recovery on same tick
  Added: Warning skip — memory jumping from <80% directly to >=100% skips warning tier entirely
  Added: Supervisor integration asymmetry table — tick return check, onRestart, output prefix, persist, dashboard observability
  Added: BrokerSupervisor tick() return discarded — threshold exceeded returns false but value ignored, only log output
  Added: BrokerSupervisor memory not observable from dashboard — no persist() method, stats only in logs
  Added: Peak memory tracking side-effect — getMemoryStats() updates peakMemoryUsage, persist() is primary tracker
  Added: Runtime Config Behavior section — hot-reconfiguration, gc_interval mid-flight change, loopIteration not reset
  Added: Tuning guidelines expanded — gc_interval controls effective grace period, threshold_mb chunk granularity, restart_delay_seconds effective delay formula
  Added: Error handling expanded — gc_interval=0 DivisionByZeroError, threshold_mb=0 always triggers, null output callback silent, freed 0.00 MB scenario
  Added: Grace Period Quantization sequence diagram showing 100s actual delay with default settings
  Added: Enhanced Tick Decision Flow diagram with config() reads, peak update, 80-100% range annotation
  Added: Key Components table expanded with output() method, constructor initial peak
  Skipped: Outline publish (plans/doc.md does not exist)
  Remaining opportunities: deepen rate-limiting or http-api-reference docs

2026-03-27 | rate-limiting deepening
  Updated: docs/publishing/rate-limiting.md
  Added: Throttle strategy execution-continues bug — release() does not stop handle(), message publishes immediately AND duplicate job is queued
  Added: Double counting analysis — facade attempt() + job allows()+hit() = 2 hits per message, effective limit is ~50% of configured
  Added: TOCTOU race condition — allows()+hit() is non-atomic, concurrent workers can exceed limit by 1
  Added: Config resolution per call — 8 config() calls on allowed path, 14+ on reject path, no caching after construction
  Added: Singleton cache driver lock — constructor freezes cache driver for process lifetime via ServiceBindings singleton
  Added: clear() bypasses isEnabled() — only public method without the guard, inconsistent with allows/hit/remaining/availableIn
  Added: attempt() internal flow — allows() called twice on reject path (once by checkRateLimit, once by attempt internally)
  Added: handleRateLimitExceeded return value discarded — facade callers ignore the int return on throttle path
  Added: Unknown strategy string falls through to throttle behavior — no validation
  Added: availableIn() returns 0 when not exceeded — only calculates delay for exceeded windows
  Added: Key Components table expanded with RateLimitExceededException accessors, MqttBroadcast::publish(), MqttBroadcast::publishSync()
  Added: Error handling table expanded with throttle continues, double counting, TOCTOU, unknown strategy, clear() without guard
  Added: Mermaid diagrams — Rate Check Flow updated with throttle bug showing handle() continuation, Double Counting async lifecycle sequence diagram, attempt() internal flowchart
  Skipped: Outline publish (plans/doc.md does not exist)
  Remaining opportunities: deepen http-api-reference docs

2026-03-27 | http-api-reference deepening
  Updated: docs/dashboard/http-api-reference.md
  Added: Memory key mismatch — HealthController and DashboardStatsController read $data['memory'] but repository stores memory_mb, always yields 0
  Added: DashboardStatsController::calculateUptime() sign bug — now()->diffInSeconds($startedAt, false) produces negative result, reversed vs HealthController
  Added: Queue::size() error handling asymmetry — DashboardStatsController has try/catch, HealthController does not (throws on sync driver)
  Added: destroy() returns [] body despite 204 status — not a true empty-body response
  Added: JSON decode redundancy — 4 json_decode per message in index(), 3 in show(), with Mermaid diagram
  Added: Throughput data point off-by-one — inclusive loop produces 61/25/8 points not 60/24/7, with Mermaid diagram
  Added: LIKE wildcard characters % and _ not escaped in topic filters — matches broader than intended
  Added: retry() and retryAll() drop retain flag — retried messages default to retain:false
  Added: Metrics rate dilution — divides by constant denominator (60/24/7) not actual elapsed time
  Added: BrokerController::show() loads all() then firstWhere('id') — no findById() on repository
  Added: BrokerController::index() O(n) message queries — 2N+1 queries when logging enabled
  Added: null last_heartbeat_at handling — PHP_INT_MAX fallback → disconnected status
  Added: Route ordering — retry-all before {id}/retry to avoid wildcard capture
  Added: formatMessage()/formatJob() byte-level strlen/substr truncation — multibyte UTF-8 split risk
  Added: FailedJobController::index() unfiltered COUNT(*) on every request for meta.total
  Added: FailedJobController::flush() uses truncate() — resets auto-increment, bypasses model events
  Added: Key Components table expanded from 8 to 24 entries with per-method detail and bug annotations
  Added: Enhanced Mermaid diagrams — health check with memory key mismatch annotation, retry flow with retain dropped, JSON decode redundancy flowchart, throughput gap-fill inclusive loop
  Added: Error handling table expanded with Queue::size() throw, LIKE wildcards, retain drop, negative uptime, destroy() body
  Skipped: Outline publish (plans/doc.md does not exist)
  All docs have been deepened — no remaining undocumented features or un-deepened docs
